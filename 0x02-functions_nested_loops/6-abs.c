#include "holberton.h"
/**
*_abs - print the absolute value integer (number)
*@number: - value of number
*Return: absolute value
**/
int _abs(int number)
	{
	if (number < 0)
		{
		number = number * -1;
		}
return (number);
}
